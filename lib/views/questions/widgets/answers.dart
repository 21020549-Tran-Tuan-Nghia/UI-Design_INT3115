import 'package:flutter/material.dart';

class QuestionAction extends StatelessWidget {
  const QuestionAction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}